"""Famille H — Cross-layer (DOC/CATALOGUE §H).

H1 résidu inter-layers ‖A_ℓ − A_ℓ₊₁‖_F, H2 composition A_ℓ × A_ℓ₊₁,
H3 trajectoire r_eff(ℓ), H4 convergence layer→∞.

Scope = "per_regime_layers" : la Property reçoit toute la pile de couches
d'un régime pour analyser leur dynamique cross-layer.
"""

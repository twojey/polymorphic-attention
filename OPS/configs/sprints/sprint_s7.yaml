# Sprint S7 — LL Oracle (Llama-3.2-1B ou MinimalLM local).

defaults:
  - base
  - _self_

sprint_id: S7_ll

backend:
  use_hf_llama: true
  hf_model_name: meta-llama/Llama-3.2-1B
  local_checkpoint: null

battery:
  level: research
  n_examples_per_regime: 64

device: cuda
